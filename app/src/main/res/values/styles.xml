<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--set content transition also here-->
        <item name="android:windowContentTransitions">true</item>
    </style>


    <!--TODO important parameter to set for animation-->
    <style name="AppTheme.Home">
        <!--for single element transition
            implement the theme to the parent activity
        <item name="android:windowEnterTransition">@transition/grid_exit</item>-->
        <item name="android:windowSharedElementEnterTransition">@android:transition/fade</item>
        <item name="android:windowReenterTransition">@android:transition/slide_top</item>
    </style>

    <style name="TextStyle">
        <item name="android:textColor">#FAFAFA</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>
</resources>
